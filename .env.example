# Example env file — copy to `.env` and fill with your real keys (do NOT commit .env)
# iOS key (used when building an iOS binary / dev client)
GOOGLE_MAPS_API_KEY_IOS=YOUR_IOS_GOOGLE_MAPS_API_KEY

# Android key (used by Android builds / Expo Go when configured)
GOOGLE_MAPS_API_KEY_ANDROID=YOUR_ANDROID_GOOGLE_MAPS_API_KEY
